spring:
  flyway:
    enabled: true
    # location은 main 설정과 동일하게 가지만, url/user/pass는 Testcontainers에 의해 덮어씌워집니다.
    locations: classpath:db/migration
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true
    show-sql: true
    
logging:
  level:
    org.springframework.test.context.transaction: TRACE
    org.testcontainers: INFO

# 테스트 컨텍스트 로드용 이더리움 더미 설정
ethereum:
  rpc-url: http://localhost:8545
  rpc-url-docker: http://localhost:8545
  private-key: 0x4f3edf983ac636a65a842ce7c78d9aa706d3b113bce9c46f30d7d21715b23b1d

